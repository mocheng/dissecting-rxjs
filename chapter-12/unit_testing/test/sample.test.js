describe('sample suite', () => {
  it('should work', () => {
  });

  it('should work as well', () => {
  });

  context('another suite', () => {
    it('should do something', () => {
    });
  });
});


